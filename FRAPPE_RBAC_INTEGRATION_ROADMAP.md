# 🔐 프라뻬(Frappe/ERPNext) 동적 권한 동기화 & 지점별 자율 제어 개발 로드맵
**문서 상태:** 기획 및 대기 (Pending - 대표님 프라뻬 권한 분리 작업 완료 후 즉시 구현)  
**작성일자:** 2026년 08월 22일  
**관련 모듈:** `roles.py`, `erpnext_tools.py`, `agent.py`, `config_manager.py`

---

## 🎯 1. 아키텍처 개요 및 목표

### 【핵심 원칙】
1. **전사 통합 룰 (Global Master Rules):**  
   * 오너 / 최고관리자(대표님)만 등록·수정 가능. (전사 마진율, 임계 금액 한도, 시스템 전반 가드레일)
2. **지점별 로컬 권한 & 기초 데이터 변경 (Branch-Level Control):**  
   * 인공지능(삼돌이)이 **프라뻬(Frappe/ERPNext) 백엔드의 User Role 및 Warehouse Permission을 실시간으로 읽어와서 동기화**.
   * 각 지점장(Monse, Nadya 등)은 프라뻬에서 자신에게 부여된 **소속 지점 범위 내에서만** 기초 데이터 변경, 지점 전용 안내 문구 설정, 단골 단가 조정 등을 메신저로 처리 가능.

---

## 🏢 2. 3계층 권한 구조 (3-Tier RBAC)

```
┌────────────────────────────────────────────────────────────────────────┐
│ Level 1: 오너 / 최고관리자 (Owner / System Manager)                     │
│  • 전사 글로벌 룰북 학습 및 수정                                       │
│  • 전 창고 원가, 상세 재고, 매출 원장 전체 열람 및 수정                │
│  • 전 지점 채널 매트릭스 및 메신저 연동 키 관리                        │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│ Level 2: 지점 매니저 (Branch Manager / Stock Manager)                  │
│  • 프라뻬 User Permission에 할당된 지점(예: IKEA, TIENDA) 권한 자동 연동 │
│  • 해당 지점의 가용 재고 수정, 지점 단골 고객 할인율 설정             │
│  • 본사 ➔ 내 지점 재고 이동(Material Transfer) 요청 및 수령 확인      │
│  • 타 지점 데이터나 전사 룰북은 변경 불가 (자동 차단)                  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│ Level 3: 일반 직원 & 고객 (Staff / Customer)                           │
│  • 단순 가용 재고 조회 및 주문서(Sales Order) 생성 요청만 가능         │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 3. 프라뻬 권한 분리 완료 후 구현할 태스크 목록 (Next Phase TODOs)

### ① 프라뻬 실시간 권한 조회 API (`erpnext_tools.py`)
* `get_frappe_user_permissions(phone_number)`
  * 전화번호(`mobile_no`)로 Frappe `User` 및 `Employee` 조회
  * 할당된 `Roles` (`System Manager`, `Stock Manager`, `Branch Manager` 등) 추출
  * `User Permission` 테이블에서 허용된 `Warehouse` 목록(`[SUB] IKEA - K` 등) 실시간 로드

### ② 동적 권한 매핑 엔진 (`roles.py` 고도화)
* 기존의 정적 전화번호 매핑을 **Frappe 백엔드 실시간 권한 동기화 + 5분 로컬 캐싱** 구조로 승격.
* 프라뻬에서 직원의 지점을 변경하거나 퇴사 처리하면 메신저 삼돌이 권한에도 0.1초 만에 즉시 반영.

### ③ 지점 매니저 전용 로컬 데이터 변경 도구 (`agent.py`)
* `update_branch_settings(branch, setting_key, value)`
  * 발신자가 해당 지점 매니저 권한을 가졌는지 검증 후 실행.
  * 소속 지점의 안내 문구, 지점 연락처, 지점 로컬 룰 적용.

---

## 📌 4. 진행 현황
- [x] 전사 통합 룰북 오너 전용 보안 잠금 완료
- [x] 프라뻬 동적 권한 연동 설계서 작성 완료 (`FRAPPE_RBAC_INTEGRATION_ROADMAP.md`)
- [ ] **(대기 중)** 대표님의 프라뻬 백엔드 권한 분리 작업 완료 후 실시간 API 연결 구현 예정
